#include <iostream>
#include <string>
#include <fstream>
#include <string>
#include <vector>
#include <array>

#ifndef LVLS_IN_RANGE
#define LVLS_IN_RANGE

bool lvlsInRange(std::vector<std::array<std::string, 4>> logBase, std::ofstream& out);

#endif